{
  "name": "@handyman/core",
  "version": "0.1.0",
  "type": "module",
  "main": "dist/index.js",
  "module": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "bunx --bun tsup src/index.ts --format esm,iife --global-name Handyman --out-dir dist --dts --clean",
    "dev": "tsup src/index.ts --format iife --global-name Handyman --out-dir dist --watch"
  },
  "dependencies": {
    "@zumer/snapdom": "^2.15.0"
  },
  "devDependencies": {
    "@happy-dom/global-registrator": "^20.10.6",
    "@types/bun": "^1.3.14",
    "tsup": "^8.0.0",
    "typescript": "^5.6.0"
  }
}
